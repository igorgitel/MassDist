# State of the runs

Generated by `sync_to_git.py`.  The date of this state is the date of the
commit that carries it.

The `.npz` files themselves are not in this repository -- they are outputs,
and they are reproduced by running the corresponding cell in the run
notebook.  What is recorded here is the parameters they were produced with
and the numbers that came out, which is what a later reader needs in order
to know which state of the code this commit describes.

### open_coagulation_conservative_geometric.npz

unreadable: No module named 'numpy'

### open_fragmentation_conservative_geometric_f0.00.npz

unreadable: No module named 'numpy'

### open_fragmentation_conservative_geometric_f0.30.npz

unreadable: No module named 'numpy'
